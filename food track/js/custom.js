document.addEventListener('DOMContentLoaded', () => {
// aos 플러그인
  "use strict";

window.addEventListener('load', () => {
  AOS.init({
    duration: 1000,
    easing: "ease-in-out",
    once: false,
    mirror: false
  });
});






});